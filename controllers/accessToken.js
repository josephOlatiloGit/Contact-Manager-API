// const jwt = require("jsonwebtoken")
// function generateAccessToken(user) {
//     const payload = {
//         id: user._id,
//         email: user.email,
//     };
//     const secret = 'chimozuki123';
//     const options = { expiresIn: '10m' };

//     return jwt.sign(payload, secret, options);
// }

// module.exports = generateAccessToken()